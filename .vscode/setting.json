// Workspace settings
{
    // The following will hide the js and map files in the editor
    "files.exclude": {
        "**/dist": false,
        "yarn.lock": true,
        "npm-debug.log": true
    }
}