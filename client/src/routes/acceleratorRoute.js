import React, { Component } from 'react';
import { Route, IndexRoute } from 'react-router';

import Accelerator from '../components/home/accelerator';

const AcceleratorRoute = {
    path: '/accelerator',
    component: Accelerator
}

export default AcceleratorRoute;